max_obstacle_height: 0.60  # assume something like an arm is mounted on top of the robot

# Obstacle Cost Shaping (http://wiki.ros.org/costmap_2d/hydro/inflation)
robot_radius: 0.20  # distance a circular robot should be clear of the obstacle (kobuki: 0.18)
# footprint: [[x0, y0], [x1, y1], ... [xn, yn]]  # if the robot is not circular

# map_type: voxel (deprecated now)

obstacle_layer:
  obstacle_range: 4.9 # Needs to be < raytrace_range it seems
  raytrace_range: 5.0
  enabled:              true
  max_obstacle_height:  1
  origin_z:             0.0
  z_resolution:         0.2
  z_voxels:             2
  unknown_threshold:    15
  mark_threshold:       0
  combination_method:   1
  track_unknown_space:  false    #true needed for disabling global path planning through unknown space

  publish_voxel_map: false
  observation_sources:  scan bump collisions
  scan:
    data_type: LaserScan
    topic: /robot0/laser_0
    marking: true
    clearing: true
    min_obstacle_height: 0.0
    max_obstacle_height: 1
  bump:
    data_type: PointCloud2
    topic: mobile_base/sensors/bumper_pointcloud
    marking: true
    clearing: false
    min_obstacle_height: 0.0
    max_obstacle_height: 0.15
  collisions:
    data_type: PointCloud2
    topic: /pips_collisions
    marking: true
    clearing: false
    min_obstacle_height: 0.0
    max_obstacle_height: 0.6
    
  # for debugging only, let's you see the entire voxel grid

static_layer:
  enabled:              true
  map_topic:            /known_map
  
inflation_layer:
  inflation_radius:     0.6  # max. distance from an obstacle at which costs are incurred for planning paths.
  cost_scaling_factor:  10.0 # exponential rate at which the obstacle cost drops off (default: 10)


